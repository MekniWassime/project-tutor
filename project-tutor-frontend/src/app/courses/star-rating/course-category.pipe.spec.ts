import { CourseCategoryPipe } from './course-category.pipe';

describe('CourseCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
